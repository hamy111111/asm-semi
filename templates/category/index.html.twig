{% extends 'base.html.twig' %}

{% block body %}
    <a class="btn btn-success" href="{{ path('category_create') }}">Add Category</a>
    <div class="bs-example" data-example-id="striped-table">
        <table class="table table-striped">
            <thead>
            <tr>
                <th>#</th>
                <th>Name</th>

            </tr>
            </thead>
            <tbody>
            {% for category in categories %}
                <tr>
                    <th scope="row">{{ category.id }}</th>
                    <td>{{ category.name }}</td>

                    {#                    <td> <a href="{{ asset('upload/image/' ~ product.image) }}">View image</a><td>#}
                    <td>
                        <a href="{{ path('category_details', {'id': category.id}) }} " class='btn btn-success'>View</a>
                                                <a href="{{ path('category_edit', {'id': category.id}) }}" class='btn btn-default'>Edit</a>
                        <a href="{{ path('category_delete', {'id': category.id}) }}" class='btn btn-danger'>Delete</a>
                        {#                        <a href="{{path('product_details',{'id':product.id})}} " class='btn btn-success'>View</a>#
                        {#                        <a href="#" class='btn btn-default'>Edit</a>#}
                        {#                        <a href="{{ path('product_delete', {'id': product.id}) }} "class='btn btn-danger'>Delete</a>#}
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}

